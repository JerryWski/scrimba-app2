import styles from "./Card.module.css"
import Header from "./Header";

const Card = () => {
    return (
        <div className={styles.card}>
             <Header />
        </div>
    )
};

export default Card;